{
  "id": "connectors",
  "version": "0.6.5-rc.3",
  "name": "OpenFactory Connectors",
  "description": "Installs OpenFactory Connectors",
  "options": {
    "shdr-gateway-version": {
      "type": "string",
      "default": "v0.6.5rc3",
      "description": "Git ref of the SHDR Gateway image to install"
    },
    "shdr-coordinator-version": {
      "type": "string",
      "default": "v0.6.5rc3",
      "description": "Git ref of the SHDR Coordinator image to install"
    },
    "opcua-gateway-version": {
      "type": "string",
      "default": "v0.6.5rc3",
      "description": "Git ref of the OPC UA Gateway image to install"
    },
    "opcua-coordinator-version": {
      "type": "string",
      "default": "v0.6.5rc3",
      "description": "Git ref of the OPC UA Coordinator image to install"
    },
    "useLocalSdk": {
      "type": "boolean",
      "default": false,
      "description": "Use local SDK (mounted in workspace) instead of GitHub"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}

{
  "id": "infra",
  "version": "0.5.3",
  "name": "OpenFactory SDK Feature",
  "description": "Installs the OpenFactory SDK and its local infrastructure.",
  "options": {
    "openfactory-version": {
      "type": "string",
      "default": "v0.5.3",
      "description": "Git ref (e.g. v1.2.3 or main) of OpenFactory to install"
    },
    "useLocalSdk": {
      "type": "boolean",
      "default": false,
      "description": "Use local SDK (mounted in workspace) instead of GitHub"
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}

apiVersion: 1

providers:
  - name: OpenFactory
    orgId: 1
    folder: OpenFactory
    type: file

    disableDeletion: false
    allowUiUpdates: true

    options:
      path: /etc/grafana/provisioning/dashboards
